<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="springblog.mapper.MemberMapper">
    <!--회원가입-->
    <insert id="insertMember" parameterType="Member">
        <selectKey keyProperty="memberId" resultType="Long" order="BEFORE">
            SELECT SEQ_MEMBER_ID.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO MEMBER (
            MEMBER_ID
            , LOGIN_ID
            , EMAIL
            , PASSWORD
            , ROLE
            , ACCOUNT_NON_EXPIRED
            , ACCOUNT_NON_LOCKED
            , CREDENTIALS_NON_EXPIRED
            , ENABLED
            , CREATED_AT
            , UPDATED_AT
        ) VALUES (
            #{memberId}
            , #{loginId}
            , #{email}
            , #{password}
            , #{role}
            , 'Y'
            , 'Y'
            , 'Y'
            , 'Y'
            , SYSTIMESTAMP
            , SYSTIMESTAMP
        )
    </insert>

    <!--login id 로 회원 찾기-->
    <select id="selectMemberByLoginId" parameterType="String" resultType="Member">
        SELECT
            MEMBER_ID
            , LOGIN_ID
            , EMAIL
            , PASSWORD
            , ROLE
            , ACCOUNT_NON_EXPIRED
            , ACCOUNT_NON_LOCKED
            , CREDENTIALS_NON_EXPIRED
            , ENABLED
            , CREATED_AT
            , UPDATED_AT
        FROM MEMBER
        WHERE LOGIN_ID = #{loginId}
    </select>

    <!--email id로 회원 찾기-->
    <select id="selectMemberByEmail" parameterType="String" resultType="Member">
        SELECT
            MEMBER_ID
            , LOGIN_ID
            , EMAIL
            , PASSWORD
            , ROLE
            , ACCOUNT_NON_EXPIRED
            , ACCOUNT_NON_LOCKED
            , CREDENTIALS_NON_EXPIRED
            , ENABLED
            , CREATED_AT
            , UPDATED_AT
        FROM MEMBER
        WHERE EMAIL = #{email}
    </select>
</mapper>
